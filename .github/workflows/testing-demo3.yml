name: Test Demo 3

on:
  pull_request:
    paths:
      - 'demo3/**'
      - '.github/**'
    branches: [ main ]

  workflow_dispatch: {}

jobs:
  check-if-test-should-run:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.watched_paths.outputs.src }}
    steps:
      - id: watched_paths
        uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3.0.2
        with:
          filters: |
            src:
            - demo3/**
            - build.gradle.kts
            - settings.gradle.kts
            - gradle/**
      - name: write build should run in env
        run: |
          echo ${{ steps.watched_paths.outputs.src }}
  test-demo-3:
    needs: check-if-test-should-run
    if: ${{ needs.check-if-test-should-run.outputs.should_run == 'true' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./demo3
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'liberica'
          java-version: '21'
      - run: ./gradlew clean build
